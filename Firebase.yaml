hosting:
  public: public
  ignore:
    - firebase.yaml
    - '**/.*'
    - '**/node_modules/**'
  rewrites:
    - source: '**'
      destination: '/index.html'

emulators:
  functions:
    port: 5001
  firestore:
    port: 8080

# Other configuration settings for your Firebase project...
